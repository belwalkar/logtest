This code parses the log file and collects data
1. flow_log.txt contains log data
2. lookup_table.csv contains lookup data in teh log parsing
3. log.py contains the log parsing code
4. To run python3 log.py

